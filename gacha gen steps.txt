gacha_detail: sets rate of each card (PER), whether it's shown on the event page (TOP), and the BG
gacha_detail_daily: same as above, but for daily 6x paid jewel pull
gacha_main: sets the gacha type, name, image, description text, start/end times, cost, details to use, etc. higher IDs take priority


rough steps for preparing gacha data will go here (wip).
parsing/verification steps removed because implementation is complete and takes priority.


calculating detail sheet "PER":
  PER is like a big spinner - each card has that many positions that give you the card.
  For example, card 1 has PER=1, card 2 has PER=2, card 3 has PER=3.
  Card 1 has a 1/6 chance. Card 2 has 2/6. Card 3 has 3/6. (6 equals 1+2+3, the total of all PERs)
  So...:
    1. Calculate *exact* odds of each card as a fraction
         e.g. 1% limited UR odds: 1/(100*number of limited URs)
    2. Build a set of all denominators, find least common multiple of them
    3. Assert LCM is a sane value (e.g. fits in s16 integer)
    4. Convert frations to have LCM as denominator, take numerator as PER


banner image generation:
  Gacha banners are 500x120 PNG files.
  Start by filling with the set background.
  Then use colorgram.py to extract a palette for text.
  Then place the evolved artwork of each (or a random selection of) limited cards over it.
  Then create text overlays ("プレミアムガチャ" and the banner text, plus "◯曜日まで").

